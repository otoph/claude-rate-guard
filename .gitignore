# Runtime state / logs (never commit usage data)
rate_limit_state.json
*.tee.log
*.log

# Personal / local Claude Code config & tooling (keep private out of the repo;
# the public-facing template lives at examples/CLAUDE.md.snippet)
CLAUDE.md
.claude/

# Internal development artifacts (backlog, session dev-logs — not for the public repo)
/dev/

# Secrets / environment (defensive: never publish credentials)
.env
.env.*
*.pem
*.key

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
